# ATRS Implementations

| implementation | status | notes |
| --- | --- | --- |
| CrawlDex | reference | Emits `atr_version: "0.1"` records from the public trust-record API, supports the optional boolean `removed_in_batch` decision echo for confirmed batch aborts or reroutes, and validates server output against this package in the monorepo test suite. |

No other implementation is listed yet.
